A function that takes as input as a list/array of integers as input and returns the index and value of the greatest element.
```python
lst = input("Enter a list/array: ")
def function (lst):
	maximum = max(lst)
	index = lst.find(maximum)
	
	return index, maximum
```
![[Screenshot 2026-09-05 at 09.48.37.png]]
[Large software system structure]

This course is and introduction to build large software.
learn essential tools for systems developers:
- linux, command line, bash, git...

